// variables4.nr
// use the `hint` watch subcommand for a hint.


fn main() {
    let mut x = 3; //needs to be assign as mutable
    let str1 = f"x is {x}";
    println(str1);
    x = 5; // don't change this line
    println(f"x is now {x}");
}
